#Azure pipeline sample

trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

stages:
- stage: Build
  displayName: 'Build Stage'
  jobs:
  - job: BuildJob
    displayName: 'BuildJob'
    steps:
    - script: echo 'from build'
  - job: DeployJob
    displayName: 'Deploy Job'
    steps:
    - script: echo 'from deploy'
