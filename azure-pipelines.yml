#Azure pipeline sample

trigger:
- main

pool:
  vmImage: 'ubuntu-lastest'

stages:
- stage: Build
  displayName: 'Build Stage'
  jobs:
  - job: BuildJob
    displayName: 'BuildJob'
    steps:
    - script: echo 'test'